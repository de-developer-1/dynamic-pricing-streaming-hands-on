# Dynamic Pricing with Streaming Data

This project simulates dynamic pricing using streaming data.

## Structure

- `src/`: Source code
- `notebooks/`: Exploratory notebooks
- `tests/`: Unit tests

## Usage

Use Docker Compose to run the environment:
```bash
docker-compose up --build
```